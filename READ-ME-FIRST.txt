Copy each file over the file at the SAME path in kristian-davis-portfolio, then commit and push
to main. Nothing else in the repo changes. Images are untouched.

  index.html                     js/main.js
  link.html                      js/case-study.js
  css/style.css                  js/link.js
  css/case-study.css             case-studies/nitefind.html
  css/link.css                   case-studies/smiteforge.html
                                 case-studies/zentra.html
                                 case-studies/amun.html

Suggested commit message:

  Bio copy + layout, per-project title hover, one continuous scroll, mobile nav dropdown,
  WCAG AAA pass, unified 1280 page grid

--------------------------------------------------------------------------------------------
CONTENT
--------------------------------------------------------------------------------------------
- Bio section copy restored to the edited version: heading "The thinking behind the work."
  plus the three rewritten paragraphs.
- "the decision" / "the open question" copy replaced with short lorem ipsum on all four
  projects, sized for the shorter real copy to come.

--------------------------------------------------------------------------------------------
LAYOUT
--------------------------------------------------------------------------------------------
- Bio rebuilt. The skills list moved out of the photo column and now sits under the body copy,
  so the left column reads heading, rule, paragraphs, skills. The photo column stretches to
  exactly the height of that copy and takes an equal share of the width. Mobile stacks the
  photo last, below the copy and the lists; tablet gets a 3/2 crop so the photo does not tower
  over the text; phones get 4/4.4.
- One page grid. Nav, hero, work rows, bio and contact all resolve to the same 1280 column with
  the same clamp(24px,5vw,64px) gutter. Before this the work rows ran in a 1420 column and the
  hero had no column at all, so past ~1400px three different left edges were visible at once.
- Section rhythm evened out: work->about was a 74px gap and about->contact 138px. One value now,
  split across the two adjoining sections.
- Hero: the second phrase sits the same distance from the line below it as from the phrase
  above it. The reflection is foreshortened to half height and anchored at the waterline, so
  the sub-line and SEE THE WORK are inside the first viewport on load at every size tested.
  On mobile the sub-line to CTA gap went from 26px to 35px.

--------------------------------------------------------------------------------------------
INTERACTION
--------------------------------------------------------------------------------------------
- Scroll snapping removed entirely (it was scroll-snap-type:y proximity on html plus
  scroll-snap-align on the sections). That was what pulled a swipe back to a section edge
  instead of letting it carry. One continuous smooth scroll now.
- Mobile nav collapses into a single dropdown, with the project list as a second dropdown
  nested inside it. Closes on tap-out, Escape, link click, or crossing back to desktop.
- Work band titles hover to their own project colour instead of the site purple.

--------------------------------------------------------------------------------------------
ACCESSIBILITY  (target: WCAG 2.2 AAA)
--------------------------------------------------------------------------------------------
Contrast, measured against the #050505 page:
  small mono labels   #7d786f  4.65:1  ->  #a8a49b  8.20:1
  Nitefind label      #b04ad6  4.66:1  ->  #cf82ea  7.80:1
  Amun label          #8f8f8f  6.30:1  ->  #a0a0a0  7.79:1
  link underlines,
  control borders     #3a3a38  1.79:1  ->  #63625c  3.33:1   (1.4.11 non-text, needs 3:1)

  The brand purple #a06bff (5.86:1) is unchanged on decoration and on LARGE text, where AAA
  asks 4.5:1 and it passes: the hero headline, the case-study stat figures, the ghost words.
  A lighter --accent-text (#b48cff, 7.90:1) is used ONLY where accent lands on small body-size
  text, which AAA holds to 7:1. Per-page accents carry their own --accent-text.

Other:
  - Every pointer target is now at least 44x44 (2.5.5). Desktop nav was 21-27px tall. The height
    is added as padding-top on the links themselves, not a wrapper, so the box really is the
    target and each link's hover underline stays under the words.
  - Added <main>. Added a screen-reader-only "Selected work" H2 to fix an H1 -> H3 skip, and
    made the bio's opening line a real H2. Outline is now H1 > H2 > H3 throughout.
  - Focus ring thickened to 3px with a dark halo so it survives any background it lands on.

--------------------------------------------------------------------------------------------
VISUAL
--------------------------------------------------------------------------------------------
- All four ghost words render in the brand purple (WORK was white) at 1.5px stroke, opacity .55
  (was 1px at .30-.35) so the letterforms actually read. Same on the case-study pages.

--------------------------------------------------------------------------------------------
VERIFIED AT
--------------------------------------------------------------------------------------------
390, 430, 768, 900, 1024, 1440, 1920. No horizontal overflow at any width. Nav and section
content share a left edge at every width. Hero sub-line and CTA above the fold at every width.
Zero AAA contrast failures. No console errors.
